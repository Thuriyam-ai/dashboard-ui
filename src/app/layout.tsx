import type { Metadata } from 'next';
import { inter } from './fonts';
import './globals.css';
import { ContextWrapper } from '@/app/context-wrapper';

export const metadata: Metadata = {
  title: 'WorkIndia Next Starter Kit',
  description: 'Generated by wi-next-starter',
};

/**
 * Root layout component for the application.
 * This component wraps the entire app and applies global styles, fonts, and structure.
 * @param props - The props for the RootLayout component.
 * @param props.children - The child components to be rendered within the layout.
 * @returns The layout component with global fonts and styles applied.
 */
export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={inter.variable}>
      <body>
        <ContextWrapper>{children}</ContextWrapper>
      </body>
    </html>
  );
}
